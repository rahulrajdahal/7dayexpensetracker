name: Cypress Tests

on: push

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        # Install NPM dependencies, cache them correctly
        # and run all Cypress tests
      - name: Cypress run
        uses: cypress-io/github-action@v6
        env:
          CYPRESS_DATABASE_URL: postgresql://rahulrajdahal:THKY8es6oQmB@ep-broad-hat-a59p1z0d.us-east-2.aws.neon.tech/7dayexpensetracker-test?sslmode=require
        with:
          build: npm run build
          start: npm start
